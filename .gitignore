# Session-specific data (local only)
debug/
file-history/
image-cache/
session-env/
shell-snapshots/
todos/
projects/
history.jsonl  # Now in dedicated claudeHistory repo
statsig/
plugins/  # Machine-specific runtime state (marketplace timestamps, caches, installs)

# Local settings overrides
settings.local.json

# Backup files
*.backup
*.swp
*.swo
*~

# OS files
.DS_Store
.update.lock
