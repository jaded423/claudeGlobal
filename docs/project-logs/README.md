# Project Session Logs

This directory contains detailed session logs for each project, created by the `/log` command.

Each project gets its own log file: `[project-name].md`

These provide a historical record of Claude Code sessions across all projects.
